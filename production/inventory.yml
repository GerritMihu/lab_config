---
# Inventory for 8 Allzweck-PCs (General Purpose Workshop PCs)
# Accessible via: ap1@ap1.local, ap2@ap2.local, ..., ap8@ap8.local
# This is a backup/alternative inventory file. The primary inventory is hosts.yml

all:
  children:
    workshop_pcs:
      children:
        all_purpose:
          hosts:
            ap1:
              ansible_host: ap1.local
              ansible_user: ap1
              purpose: all_purpose
            ap2:
              ansible_host: ap2.local
              ansible_user: ap2
              purpose: all_purpose
            ap3:
              ansible_host: ap3.local
              ansible_user: ap3
              purpose: all_purpose
            ap4:
              ansible_host: ap4.local
              ansible_user: ap4
              purpose: all_purpose
            ap5:
              ansible_host: ap5.local
              ansible_user: ap5
              purpose: all_purpose
            ap6:
              ansible_host: ap6.local
              ansible_user: ap6
              purpose: all_purpose
            ap7:
              ansible_host: ap7.local
              ansible_user: ap7
              purpose: all_purpose
            ap8:
              ansible_host: ap8.local
              ansible_user: ap8
              purpose: all_purpose
